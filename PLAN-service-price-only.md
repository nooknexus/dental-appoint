# แผนปรับ UI คลินิกทันตกรรม — 3 งาน

สถานะ: บันทึกแผนเท่านั้น ยังไม่ดำเนินการแก้ไขโค้ด

สภาพแวดล้อมสำหรับตรวจสอบ: Frontend `http://localhost:3000` และ API `http://localhost:3001`

## 1. หน้าบริการแสดงเฉพาะราคาในบริเวณข้อมูลราคา

- นำข้อความ “ระยะเวลานัดหมายประมาณ … นาที” และตัวคั่นออก ให้เหลือ “ราคา …”
- พบ route ปัจจุบันเป็น `/services` (ผู้ใช้เรียก `/service`) ใช้ `ServicesPage` และ `PublicServiceCard` ซึ่งแสดง `description` จาก API คู่กับราคา
- ปรับเฉพาะการแสดงผล คงชื่อบริการ เนื้อหาส่วนอื่น ราคา ปุ่มจอง ข้อมูลระยะเวลาใน API และตรรกะจัดตารางนัดหมาย
- ตรวจสอบที่ `http://localhost:3000/services` ว่าทุกการ์ดแสดงราคาเดิม ไม่มีข้อความเวลาหรือตัวคั่นค้าง และจัดวางเหมาะสมทั้งมือถือและเดสก์ท็อป ตรวจผลกระทบต่อจุดใช้การ์ดร่วมกันก่อนแก้ไข

## 2. ปุ่มผู้ใช้และออกจากระบบหลังเข้าสู่ระบบ

- ตรวจ session และ navigation เดิม แล้วเปลี่ยนปุ่ม “จองคิวทันตกรรม” ที่เกี่ยวข้องให้เป็นปุ่มผู้ใช้เมื่อเข้าสู่ระบบสำเร็จ พร้อมปุ่มออกจากระบบ
- ใช้ชื่อผู้ใช้และปลายทางบัญชีที่มีอยู่ ไม่เพิ่มขั้นตอนโปรไฟล์ใหม่ และใช้กลไก logout เดิม
- ตรวจสอบก่อนเข้าสู่ระบบ หลังเข้าสู่ระบบ การโหลดหน้าใหม่ และหลังออกจากระบบ ว่า UI สอดคล้องกับ session และผู้ที่ยังไม่เข้าสู่ระบบยังจองผ่านปุ่มเดิมได้

## 3. ปุ่มย้อนกลับบนหน้าผู้ป่วย

- ตรวจ route `/patient/menu` ที่ใช้งานจริง แล้วเพิ่มปุ่ม “ย้อนกลับ” ตามรูปแบบ navigation เดิม
- กรณีเข้าหน้าโดยตรง ให้กลับไปหน้าสาธารณะที่มีอยู่และเหมาะสม โดยไม่วนกลับเข้าสู่การ redirect ไปหน้า login
- ตรวจสอบทั้งการเข้าผ่านลำดับการใช้งานปกติและการเปิด URL โดยตรง รวมถึงกรณีไม่มีประวัติหน้าก่อนหน้าที่เหมาะสม

## การตรวจสอบก่อนส่งมอบเมื่อได้รับคำสั่งให้ดำเนินการ

ตรวจ diff ให้จำกัดเฉพาะสามงานนี้ ปฏิบัติตามข้อกำหนดทดสอบและการทบทวนที่ใช้บังคับ รันการตรวจที่เกี่ยวข้องและ frontend lint/build แล้วตรวจ UI กับ session จริงตามกรณีข้างต้น งานนี้ไม่รวม deploy

## สถานะปัจจุบัน

สร้างและอัปเดตเฉพาะไฟล์ Markdown นี้ ไม่มีการแก้ไขโค้ดแอปพลิเคชัน ไม่มี production changes ของงานนี้ที่ต้องย้อนกลับ และยังไม่ได้รัน test/build หรือ deploy
